{
  "name": "money-flow",
  "version": "1.4.0",
  "homepage": "https://sjx516.github.io/money-flow/",
  "private": true,
  "dependencies": {
    "@ant-design/charts": "^1.4.2",
    "antd": "^4.20.4",
    "echarts": "^5.6.0",
    "gh-pages": "^4.0.0",
    "react": "^18.3.1",
    "react-app-rewired": "^2.2.1",
    "react-dom": "^18.3.1",
    "react-error-overlay": "6.0.9",
    "react-scripts": "^5.0.1",
    "sql.js": "^1.6.2"
  },
  "devDependencies": {
    "@craco/craco": "^7.1.0",
    "jest": "26.6.0",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start:mac": "export NODE_OPTIONS=--openssl-legacy-provider && craco start",
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && craco start",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && craco build",
    "build:mac": "export NODE_OPTIONS=--openssl-legacy-provider && craco build",
    "test": "jest",
    "eject": "craco eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "repository": "https://github.com/SJX516/money-flow.git"
}
